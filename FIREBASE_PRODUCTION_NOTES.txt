FIREBASE PRODUCTION NOTES — UPDATE 7.8.8

Authentication, approved users, organization path, and synchronization behavior are unchanged.
The shared data object now includes optional Home-screen wording fields. Older saved data automatically receives the default wording, so no database reinitialization or migration is required.

LOVE QUILTS MANAGER UPDATE 7.8.53 — FIREBASE PRODUCTION NOTES

No Firestore Rules, collection paths, indexes, or saved-data schema changes are required.

UNCHANGED PATHS
- organizations/faithful-circle-love-quilts
- organizations/faithful-circle-love-quilts/settings/main
- organizations/faithful-circle-love-quilts/transactions
- organizations/faithful-circle-love-quilts/needs

COMBINED ENTRY SAFETY
- Create Need & Distribute Now changes local data in one save operation.
- The incoming inventory transaction, linked OUT distribution, and charity need share the same sourceNeedId.
- Firebase Sync already sends changed transaction and need documents through one Firestore write batch when the queued save is flushed, so the normal three-record entry is committed together.
- If internet service is unavailable, the complete entry remains saved locally and the existing pending-save queue retries it.

SYNC COMPATIBILITY
- Update 7.8.52 canonical handling for legacy Unassigned/Storage spellings is retained.
- The new NEED_IMMEDIATE_IN source type is stored as a normal transaction field and requires no rule change.
- The linked NEED_DISTRIBUTION OUT record continues to drive yearly and YTD distribution totals.

DEPLOYMENT CHECK
1. Open the laptop first after GitHub Pages publishes.
2. Confirm Update 7.8.53 and All changes synced.
3. Create one small test entry with Create Need & Distribute Now.
4. Confirm the second approved device receives the need and both linked transactions together.
